services:
  - type: web
    name: koi-hangout-hub
    env: node
    plan: free
    buildCommand: npm install pnpm && npx pnpm install --frozen-lockfile && npx pnpm run build
    startCommand: npx pnpm --filter @workspace/db run push && node artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: koi-db
          property: connectionString

databases:
  - name: koi-db
    plan: free
